import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'pokedex_ionic',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
